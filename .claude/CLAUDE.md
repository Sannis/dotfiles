## Git

Run `git` without `-C` flag when already inside the repository.

## Style

Prefer minimal emphasis markup (bold, italic) in text.
Format Markdown tables with aligned columns (pad cells with spaces so pipe characters line up).
