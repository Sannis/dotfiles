add_to_path ()
{
    if [[ "$PATH" =~ (^|:)"${1}"(:|$) ]]
    then
        return 0
    fi
    export PATH=${1}:$PATH
}

# My home local
add_to_path ~/local/bin

# Go install root
add_to_path /local/go/bin
